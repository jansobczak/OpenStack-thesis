### Deployment host
apt-get update
apt-get dist-upgrade
apt-get install aptitude build-essential git ntp ntpdate openssh-server python-dev sudo

git clone -b 18.1.5 https://git.openstack.org/openstack/openstack-ansible /opt/openstack-ansible
cd /opt/openstack-ansible/scripts
./bootstrap-ansible.sh



### Deployment hosts
echo 'bonding' >> /etc/modules
echo '8021q' >> /etc/modules

apt-get -y install bridge-utils debootstrap ifenslave ifenslave-2.6 lsof lvm2 chrony openssh-server sudo tcpdump vlan python

#reboot


### Compute host
pvcreate --metadatasize 2048 /dev/sdb
vgcreate cinder-volumes /dev/sdb